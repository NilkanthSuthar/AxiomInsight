[project]
name = "axiom-insight"
version = "0.1.0"
description = "Role-scoped document Q&A over a Chroma vector store and DuckDB SQL, with LLM query routing."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }

[tool.pytest.ini_options]
testpaths = ["tests"]
